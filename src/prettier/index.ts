import type { Config } from "prettier";

export default {
  useTabs: false,
  tabWidth: 2,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 100,
  arrowParens: "avoid",
  singleAttributePerLine: true,
} satisfies Config;
